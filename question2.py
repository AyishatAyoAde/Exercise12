tup = 'Hello'
print(len(tup))
print(type(tup))


tup = 'Hello',
print(len(tup))
print(type(tup))

#In the first set of code the string is treated as a string and the len function counts each character
# In the second code, it is a tuple so the len function counts each item of the tuple